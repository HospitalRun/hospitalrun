{
  "name": "hospitalrun",
  "version": "0.0.1",
  "description": "HosptialRun mono-repo",
  "repository": "git@github.com:HospitalRun/hospitalrun.git",
  "author": "Maksim Sink <maksim@sinik.it>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build:components": "lerna exec --scope @hospitalrun-org/components yarn build",
    "version": "lerna exec yarn version"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^3.16.4"
  }
}